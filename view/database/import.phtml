<?php

use App\Shared\Services\Registry;

use function App\Shared\Helpers\admin_url;
use function Qubus\Security\Helpers\esc_html;
use function Qubus\Security\Helpers\esc_html__;

$this->parent('plugin::SqliteAdmin/view/layout');
$this->block('sqlite-admin', function ($params) {
$r = Registry::getInstance()->get('sqlite-admin');
$tableCount = count($params['tables'] ?? []);
?>

<section class="df-sqlite-card">
    <header class="df-sqlite-card-header">
        <div>
            <h2><?=esc_html__('Import Database', $r['id']);?></h2>
            <p><?=esc_html__('Import a SQL dump into the SQLite database.', $r['id']);?></p>
        </div>

        <div class="df-sqlite-actions">
            <a class="button" href="<?= admin_url('plugin/sqlite-admin/'); ?>"><?=esc_html__('Dashboard', $r['id']);?></a>
            <a class="button" href="<?= admin_url('plugin/sqlite-admin/export'); ?>"><?=esc_html__('Export', $r['id']);?></a>
        </div>
    </header>

    <div class="df-sqlite-card-body">
        <div class="df-sqlite-panel">
            <h3><?=esc_html__('Import SQL File', $r['id']);?></h3>

            <form
                method="post"
                action="<?= admin_url('plugin/sqlite-admin/import'); ?>"
                enctype="multipart/form-data"
                class="df-sqlite-form"
            >

                <label>
                    SQL File
                    <input type="file" name="import" accept=".sql,text/sql" required>
                    <small><?=esc_html__('Upload a SQLite-compatible SQL dump file.', $r['id']);?></small>
                </label>

                <label class="df-sqlite-checkbox">
                    <input type="checkbox" name="confirm_import" value="1" required>
                    <span>
                        <strong><?=esc_html__('I understand this may modify database tables and records.', $r['id']);?></strong>
                        <small><?=esc_html__('Imports run inside a transaction when supported.', $r['id']);?></small>
                    </span>
                </label>

                <div class="df-sqlite-form-actions">
                    <button type="submit"><?=esc_html__('Import Database', $r['id']);?></button>
                </div>
            </form>
        </div>

        <div class="df-sqlite-panel">
            <h3><?=esc_html__('Import Information', $r['id']);?></h3>

            <dl class="df-sqlite-meta">
                <dt><?=esc_html__('Visible Objects', $r['id']);?></dt>
                <dd><?= esc_html((string) $tableCount); ?></dd>

                <dt><?=esc_html__('Supported Format', $r['id']);?></dt>
                <dd><?=esc_html__('SQL files only for full database imports.', $r['id']);?></dd>

                <dt><?=esc_html__('Recommended First Step', $r['id']);?></dt>
                <dd><?=esc_html__('Export a backup before importing.', $r['id']);?></dd>

                <dt><?=esc_html__('Permission Level', $r['id']);?></dt>
                <dd><?=esc_html__('Full database imports should be limited to main-site super admins.', $r['id']);?></dd>
            </dl>
        </div>
    </div>
</section>
<?php }); ?>
